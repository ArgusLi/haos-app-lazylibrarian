#!/command/execlineb -S0
# Take down the S6 supervision tree when nginx exits unexpectedly
if { /command/s6-test ${1} -ne 0 }
if { /command/s6-test ${1} -ne 256 }

/command/s6-svscanctl -t /var/run/s6/services
