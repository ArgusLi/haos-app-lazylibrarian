server {
    listen %%interface%%:%%port%% default_server;

    client_max_body_size 0;

    # CherryPy redirects GET /lazylibrarian → Location: http://127.0.0.1:5299/lazylibrarian/
    # which the browser can't follow through ingress (Docker-internal host).
    # Proxy the exact path with trailing slash directly so CherryPy never needs to redirect.
    location = /lazylibrarian {
        proxy_pass http://127.0.0.1:5299/lazylibrarian/;
        proxy_connect_timeout 30m;
        proxy_send_timeout 30m;
        proxy_read_timeout 30m;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;
        proxy_set_header Accept-Encoding "";
        sub_filter_once off;
        sub_filter_types *;
        sub_filter /lazylibrarian %%ingress_entry%%/lazylibrarian;
    }

    location / {
        add_header Access-Control-Allow-Origin *;
        proxy_connect_timeout 30m;
        proxy_send_timeout 30m;
        proxy_read_timeout 30m;
        proxy_pass http://127.0.0.1:5299;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;
        proxy_set_header Accept-Encoding "";
        sub_filter_once off;
        sub_filter_types *;
        sub_filter /lazylibrarian %%ingress_entry%%/lazylibrarian;
    }
}
